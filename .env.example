# Discord bot token
DISCORD_TOKEN=YOUR_BOT_TOKEN_HERE

# Guild ID where slash commands will be registered
GUILD_ID=YOUR_GUILD_ID_HERE

# Channel where event announcements are sent
CHANNEL_ID=YOUR_ANNOUNCE_CHANNEL_ID

# Optional: Channel where reaction-role embed is posted
ROLE_CHANNEL_ID=YOUR_ROLE_ASSIGN_CHANNEL_ID

# Saved role message ID (from first run)
ROLE_MESSAGE_ID=   # Leave empty for first run

# Polling interval in seconds (default: 60)
FETCH_INTERVAL=60
